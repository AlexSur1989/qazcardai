# Dependencies & build
node_modules
.next
out

# Git & IDE
.git
.gitignore
.cursor
.idea
.vscode
*.md
!README.md

# Env (образ собирается без секретов; в рантайме — через compose / --env-file)
.env
.env.*
!.env.example

# Logs & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Tests & coverage
coverage
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx

# OS
.DS_Store
Thumbs.db

# Prisma: клиент генерируется на этапе сборки образа
src/generated/prisma

# Метаданные Compose не нужны внутри образа (уменьшает контекст)
docker-compose*.yml
